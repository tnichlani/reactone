import React from 'react';
import PropTypes from 'prop-types';
import './Timer.css';

const Timer = () => (
  <div className="Timer">
    Timer Component
  </div>
);

Timer.propTypes = {};

Timer.defaultProps = {};

export default Timer;
