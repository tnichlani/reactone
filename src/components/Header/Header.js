import React from 'react';
import PropTypes from 'prop-types';
import './Header.css';

const Header = () => (
  <div className="Header">
    Header Component
  </div>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
